// FuncStudio.cpp : Defines the entry point for the console application.
//

#include "stdafx.h"
#include "FuncStudio.h"
#include <iostream>
#include <vector>
#include <string>

using namespace std;

int main(int argc, char * argv[])
{
	/*
	vector<int> int_vector{ 1, 2, 4, 5, 6 };
	for (int i : int_vector) {
		unsigned int result;
		factorial(i, &result);
		cout << i << ": " << result << endl;
	}
	*/
	
	cout << "( , ): " << power() << endl;

    return 0;
}

