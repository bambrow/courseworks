// TempStudio1.cpp : Defines the entry point for the console application.
//

#include "stdafx.h"
#include "Temp.h"

#include<iostream>
#include<array>
#include<vector>

using namespace std;

int main()
{
	/*
	int arr[] = { 1,2,3,4 };
	vector<int> vec = { 3,4,5,6,7 };

	cout << count_func(begin(arr), end(arr)) << endl;
	cout << count_func(vec.begin(), vec.end()) << endl;
	*/

	/*
	int a = 2;
	char b = 'c';
	struc<int> i = struc<int>(a);
	struc<char> c = struc<char>(b);
	cout << "size of struc<int>: " << sizeof(i) << endl;
	cout << "size of int: " << sizeof(a) << endl;
	cout << "size of struc<char>: " << sizeof(c) << endl;
	cout << "size of char: " << sizeof(b) << endl;
	*/

	


    return 0;
}

